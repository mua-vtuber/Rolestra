# R12-C T1~T11 구현 감사 보고서

작성일: 2026-05-03
작성자: Claude (audit 6 병렬 agent + 통합)
범위: R12-C plan tasks.json 의 Task 1 ~ Task 11 (T0 spec 갱신은 main land 완료 — 본 audit 범위 밖)
worktree tip: `e7655cd` (round 4 fix bundle 누적, branch `feat/r12-c-channel-roles`)
main tip: `f5d42bd` (T0 spec/plan/tasks.json 만 land — main push 보류 중)

## 0. Executive Summary

| 분류 | 건수 | 비고 |
|------|------|------|
| ✅ acceptanceCriteria 달성 | 9/11 task | T1 / T2 / T3 / T5 / T7(plan) / T8 / T9 / T11 본질 / T6 본질 |
| ⚠️ 부분 달성 (의미적 갭) | 4 항목 | T6 PathGuard junction realpath 미적용 / T6 mtime → content diff / T8 Radix Accordion 미사용 / T11 workflowActive runtime flag 부재 |
| ❌ 결정적 누락 (acceptance 미충족) | 3 항목 | **T4 옵션 부서 createProject 인자 propagate** / **T10 새 워크플로우 archive 동작** / T11 spec 명시 service-level state |
| 🔁 spec-plan 어긋남 | 2 항목 | **spec T7 (pause/resume) ↔ plan T7 (PromptComposer)** / T2 plan 문구 오기 (ChannelPurpose 4종 / Channel 4 새 필드) |
| 💀 dead code / dead surface | 4 항목 | T8 ChannelRail.tsx + ProjectRail.tsx + components/shell/index.ts re-export / T9 index.ts:551 dead branch / T10 ProjectEntryView.tsx (251 lines) / T11.5 메모리의 MessengerPage.test.tsx 5 fail (본 audit 미실행) |
| 미land (별 phase 이연 결정) | 1 항목 | spec T7 회의 pause/resume IPC — 사용자 결정 옵션 B (R12-W 또는 별도 phase 흡수, 2026-05-03) |

**진입 가능성 판정**: T1~T11 본질 ✅ — T12~T20 (부서 워크플로우 본격) 진입 가능. 단 T4 옵션 부서 propagate / T10 archive / T11 workflowActive 는 closeout T20 sweep 시 결정 필요 (아래 §3 정리).

---

## 1. Task 별 상세

### Task 1 — DB Migration 018

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| `channels.role TEXT` ALTER | ✅ | `src/main/database/migrations/018-channels-role-purpose-handoff.ts:40` | nullable, default NULL |
| `channels.purpose TEXT` ALTER | ✅ | `018-channels-role-purpose-handoff.ts:41` | nullable |
| `channels.handoff_mode TEXT NOT NULL DEFAULT 'check'` | ✅ | `018-channels-role-purpose-handoff.ts:42` | PRAGMA dflt `'check'` 검증 |
| `channel_members.drag_order INTEGER` ALTER | ✅ | `018-channels-role-purpose-handoff.ts:44` | INTEGER nullable |
| `providers.is_department_head TEXT NOT NULL DEFAULT '{}'` (T18 통합) | ✅ | `018-channels-role-purpose-handoff.ts:46` | T18 인프라 선반영 |
| system_general 전역 1개 row 보존 (oldest, projectId NULL) | ✅ | `018-channels-role-purpose-handoff.ts:49-56` | `created_at ASC LIMIT 1` 보존, 나머지 DELETE |
| migrations index 등록 | ✅ | `src/main/database/migrations/index.ts:29,53` | m018 17번째 entry |
| PRAGMA 검증 PASS | ✅ | `migration-018.test.ts:38-91` | 5 column 모두 단언 |
| chain-level idempotency | ✅ | `migration-018.test.ts:182-196` | 두번째 runMigrations no-op |
| seeded 3-row 시나리오 검증 | ✅ | `migration-018.test.ts:122-161` | system_general consolidation PASS |

**평가**: spec §4 Migration 단락 + plan acceptance 양쪽 100% 충족. T18 통합 (providers.is_department_head + channel_members.drag_order) 까지 단일 마이그레이션에서 처리됨. land 완료.

### Task 2 — Shared Types

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| `ChannelRole = RoleId \| null` | ✅ | `src/shared/channel-role-types.ts:12` | `RoleId import + null union |
| ChannelPurpose 정의 | 🔁 | `channel-role-types.ts:15` | **plan 문구 "ChannelPurpose 4 종" 은 오기** — spec §4 (197 line) 와 코드 모두 자유 텍스트 `string \| null`. spec 에 4종 분류는 없음. 코드는 spec 충실. |
| `HandoffMode = 'check' \| 'auto'` | ✅ | `channel-role-types.ts:18` | `ALL_HANDOFF_MODES` + `isHandoffMode` type guard + `DEFAULT_HANDOFF_MODE='check'` 동봉 |
| Channel 새 필드 | ⚠️ | `src/shared/channel-types.ts:9-22` | **plan 문구 "Channel 4 새 필드" 는 오기** — 실제로는 Channel 3 필드 (role / purpose / handoffMode) + ChannelMember 1 필드 (dragOrder) 분배. spec §4 와 정합. |
| ChannelMember.dragOrder 필드 | ✅ | `channel-types.ts:24-30` | `number \| null` (DB 컬럼 nullable 과 1:1) |
| projectId nullable 갱신 (system_general 전역화) | ✅ | `channel-types.ts:11` | 주석으로 R12-C 후 system_general 도 null 명시 |

**평가**: 코드 land 완료 ✅. **plan tasks.json T2 의 acceptanceCriteria 두 항목 (ChannelPurpose 4종 / Channel 4 새 필드) 이 spec 과 어긋남** — closeout T20 시점 plan 문구 갱신 필요 (코드는 spec 정합).

### Task 3 — ChannelRepository + ChannelService 확장

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| Repo `channels` 4 컬럼 round-trip + `channel_members.drag_order` | ✅ | `channel-repository.ts:46-49,55,95-98,117-129,250-264,354-364,374-390` | SELECT/INSERT/UPDATE 양방향 + `_UPDATABLE_COLUMNS` whitelist + `MEMBER_COLUMNS` + `rowToMember` + `listMembers` ORDER BY + `reorderMembers` 모두 커버 |
| `parseRole` unknown → throw | ✅ | `channel-repository.ts:100-107` | `isRoleId` 통과 못 하면 `Error` throw. `parseHandoffMode` 도 동일 패턴 |
| Service 4 신규 메서드 | ✅ | `channel-service.ts:600-663` | `updateRole` / `updateHandoffMode` / `reorderMembers` / `getGlobalGeneralChannel` 모두 ChannelNotFoundError 또는 ChannelError 명시 throw |
| 전역 일반 채널 lookup | ✅ | `channel-repository.ts:229-239` + `channel-service.ts:654-663` | `WHERE project_id IS NULL AND kind = 'system_general' LIMIT 1` |
| 단위 테스트 6+ PASS | ⚠️ | `channel-service.test.ts:507-676` | R12-C 섹션 = 5 describe / 13 `it()` (요건 6 초과). **그러나 worktree 의 vitest 실행 불가 — `Cannot find module @rollup/rollup-linux-x64-gnu` (rollup native module 누락). PASS 여부는 정적 검증만. Windows / 정상 환경에서 재실행 필요** |
| `channel-repository.test.ts` 별도 파일 | ❌ | `__tests__/` 디렉토리에 부재 | T3 acceptanceCriteria 의 "Repo 단위 테스트" 가 channel-service.test.ts 로 흡수됨. parseRole throw / rowToChannel 분기 직접 음성 테스트 없음 |
| `workflowActive` runtime flag 누출 | ✅ | grep 결과 0 hit | T11 이전 leakage 없음 |

**평가**: 코드 land ✅, R12-C 13 case unit test ✅. `parseRole` throw 직접 음성 테스트 부재 (방어 로직이 dead code 가 아닌지 직접 검증 안 됨). `reorderMembers` 트랜잭션 롤백 테스트 부재 (better-sqlite3 신뢰 가정). closeout 시점 보강 권장.

### Task 4 — ProjectService + ensureGlobalGeneralChannel

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| 5 부서 자동 생성 (idea/planning/design.ux/implement/review) | ✅ | `channel-service.ts:225-234` (DEFAULT_DEPARTMENT_BLUEPRINT) + `:767-807` | 디자인은 `design.ux` 단일 role 매핑 (UI/UX 묶음 의도) |
| onProjectCreated 훅 호출 | ✅ | `src/main/index.ts:310-317` | `createSystemChannels` (2) + `createDepartmentChannels` (5) 동시 호출. "2+5=7" 주석 |
| 옵션 부서 (캐릭터/배경) blueprint + service param | ✅ | `channel-service.ts:237-243` (OPTIONAL_DEPARTMENT_BLUEPRINT) + `:767-773` (`includeOptional` 옵션) | service layer 까지만 land |
| **옵션 부서 createProject 인자 propagate** | ❌ | `src/shared/project-types.ts:41-50` (ProjectCreateInput) + `src/main/index.ts:316` | **결정적 누락** — ProjectCreateInput 에 옵션 부서 필드 없음. index.ts 호출도 `createDepartmentChannels(project.id)` (옵션 인자 누락). 게임/일러스트 프로젝트 시나리오에서 사용자가 캐릭터/배경 부서 선택할 표면이 없음 |
| handoff_mode default = 'check' | ✅ | `channel-role-types.ts:21` + `channel-service.ts:787,746,386,430` | spec §4 일치 |
| ensureGlobalGeneralChannel | ✅ | `channel-service.ts:734-750` + `channel-repository.ts:225-234` | 신규 row insert 시 projectId=null |
| 마이그레이션 책임 분리 | ✅ | migration 018 가 consolidation, service 의 ensureGlobalGeneralChannel 은 fresh-install 만 처리 | 책임 분리 정확 |
| 앱 기동 시 호출 위치 | ✅ | `src/main/index.ts:281` | ChannelService 생성 직후, ProjectService + IPC 활성 전. 마이그레이션 후 lifecycle 정확 |
| ProjectService 에 부서 코드 land | ⚠️ | `project-service.ts` 0 줄 | 책임이 ChannelService + index.ts 훅으로 이동. plan T4 의 "ProjectService 5 부서 자동 생성" 표현과 어긋나지만 코드 의도된 분리. plan 문구 갱신 권장 |
| project-service.test.ts 부서 테스트 | ❌ | grep 0 hit | 테스트는 channel-service.test.ts:265-294 에 land — 파일 위치가 plan 과 다름 |
| Mock/fallback 위반 | ✅ | DEFAULT_DEPARTMENT_BLUEPRINT / OPTIONAL_DEPARTMENT_BLUEPRINT 는 카탈로그 상수 | CLAUDE.md ABSOLUTE PROHIBITIONS 위반 없음 |

**spec "디폴트 6" vs plan "5 부서" reconcile**: spec §3 의 6 = 일반 + 5 부서. plan T4 의 "5 부서 자동 생성 + 전역 일반 채널 보장" 은 spec 6 을 정확히 분해 — 일반은 전역 1개 (ensureGlobalGeneralChannel), 부서 5 만 createDepartmentChannels. 코드 / plan / spec 모두 정합.

**Critical 갭**: 옵션 부서 propagate 누락 — 사용자가 게임/일러스트 프로젝트라 선언해도 캐릭터/배경 채널 생성 경로 부재. T20 closeout 또는 별 phase 에서 land 결정 필요.

### Task 5 — SKILL.md template

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| frontmatter (name / description) | ✅ | `src/main/skills/skill-md-template.ts:82-86` | `---` / name / description / `---` |
| Gemini silent skip 방지 | ✅ | template.ts:9-13 주석 + test:18-44 | 첫 줄 `---` + name/description 필수 검증 |
| 자기 spec 합리화 방어 4 항 | ✅ | template.ts:35-40 + test:72-80 | 정확히 4 bullet, test count 검증 |
| 본문 systemPromptKo 포함 | ✅ | template.ts:90 + test:67-70 | `tpl.systemPromptKo.trim()` 직접 삽입 |
| 4+ 부서 sample 단위 테스트 | ✅ | test.ts:46-58 | `ALL_ROLE_IDS` 9 부서 전수 + 6 개별 |
| meeting-summary (system 전용) reject | ✅ | template.ts:72-74 + test:98-102 | `isRoleId()` 가드 + cast 시 throw |

**평가**: 100% 충족. land 완료.

### Task 6 — ProjectSkillSyncService

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| 9 부서 두 폴더 (`.claude/skills` + `.agents/skills`) | ✅ | `project-skill-sync-service.ts:36, 108-111` + test:36-63 | `SKILL_ROOT_DIRS` 두 root × 9 = 18 entries |
| meeting-summary 제외 | ✅ | service.ts:106-108 (ALL_ROLE_IDS 만 순회) + test:65-73 | system 전용 분리 |
| **PathGuard 봉인** | ⚠️ | service.ts:67-81, 113 | **`assertInsideProjectRoot` 자체 helper (path.relative 비교) — spec §11.9 의 "junction realpath 비교 (CA-3 TOCTOU)" 미적용**. 일반 escape 방어는 OK 지만 Windows junction 회피 불가. 정식 PathGuard (`src/main/files/path-guard.ts`) 와 다른 구현 |
| **사용자 수정 (mtime) 감지** | ⚠️ | service.ts:126-137 + test:99-110 | **mtime 이 아닌 content diff (existing === body)** — acceptance 표현과 다른 구현. 실효는 더 정확 (touch 만 했을 때 false positive 없음) — 의미는 충족 |
| force 옵션 | ✅ | service.ts:39-42, 131-136 + test:112-126 | `SyncOptions.force` |
| 5+ 단위 테스트 PASS | ✅ | test.ts (9 it-block) | first-time 4 + idempotency 1 + customisation 2 + path safety 1 |
| IPC `project:syncSkills` 등록 | ✅ | `ipc-schemas.ts:198-202, 641` + `ipc-types.ts:493-500` + `project-handler.ts:178-205` + `router.ts:422` + `main/index.ts:331-332` | schema/type/handler/router/wiring 모두 land |

**평가**: 본질 ✅. PathGuard junction realpath 비교는 closeout T20 또는 별 보강 필요 — Windows junction TOCTOU 위험. mtime → content diff 는 실효 더 정확 (acceptance 표현 갱신 권장).

### Task 7 (plan) — PromptComposer 확장

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| channelRole null skip | ✅ | `prompt-composer.ts:52-59` + test:120-148 | persona + format 만 결합 |
| channelRole 4 부서 (idea/planning/design/implement/review) 경로 단락 | ✅ | composer.ts:97, 106-113 + test:92-118 | design 4 sub-role 모두 dotted id 그대로 주입 |
| spec §11.9 양식 ("- Claude: .claude/skills/... / - Codex/Gemini: .agents/skills/...") | ✅ | composer.ts:108-111 | `[skill 경로] 이번 부서 = <id> (<label>).` + Claude/Codex/Gemini 두 줄 + "해당 파일을 읽고 내용을 그대로 따르라." |
| 4+ unit 테스트 PASS | ✅ | test.ts | 12 it-block (R12-S 6 + R12-C 6) |
| round 2 fix #3-3 fallback (commit `8fe8942`) | ✅ | composer.ts:63-81 + test:46-65 | providerRoles 에 channelRole 미포함 시 throw 대신 "이 채널은 X 부서입니다" + format. SKILL.md 경로 / 권한 표 / skill 본문 생략 |

**평가**: 100% 충족. round 2 fallback 도 정확. land 완료.

### 🔁 spec T7 (pause/resume) — 누락 + 별 phase 이연

- **spec §11.4 / §11.7** 의 T7 = "회의 일시정지 (pause / resume) IPC 우선 implement"
- **plan tasks.json** 의 T7 = "PromptComposer 확장 — 부서별 스킬 경로 prompt 주입"
- 사용자 결정 (2026-05-03 옵션 B): R12-W 또는 별도 phase 이연. 이유:
  - 부서 워크플로우 (T13~T17) 가 아직 land 안 됐고 회의는 단순 응답 위주라 pause 효용이 워크플로우 land 후에 더 큼
  - abort 버튼 (round 4 commit `e7655cd`) 이 있어 사용자 차단 없음
- **부분 land 발견**: `meeting:pause` / `meeting:resume` IPC schema/type 은 `src/shared/ipc-schemas.ts:654-655` + `ipc-types.ts:624-639` 에 D-A 시점 등록되어 있음 (handler/router 까지 land 됐는지는 본 audit 범위 밖 — T7 이연 결정 후 수동 확인 필요)
- **closeout 처리**: T20 ADR + `docs/구현-현황.md` 에 "spec §11.4 T7 (pause/resume) 미land — plan 누락 발견 (2026-05-03), R12-W 또는 별도 phase 흡수 예정. IPC schema/type 만 D-A 시점 부분 land" 명시

### Task 8 — 통합 사이드바 (3 섹션 + Accordion + persist)

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| 3 섹션 layout (general / projects / dm 위→아래) | ✅ | `src/renderer/components/shell/Sidebar.tsx:97-174` | spec §11.2 시안 그대로 |
| 섹션 사이 divider + section header | ✅ | `Sidebar.tsx:102-104,111,118-120,167` | h3 헤더 + `border-t border-border-soft` divider 2개 |
| 프로젝트 collapsible (Accordion 패턴) | ⚠️ | `src/renderer/features/sidebar/ProjectAccordion.tsx:97-152` | **Radix Accordion 미사용 — 자체 구현** (`button` + `aria-expanded` + `data-expanded` + `▼/▶` chevron). lazy mount 추가. plan "Radix Accordion 또는 동등 패턴" 허용 범위 안. 시각 검증 필요 |
| 디폴트 펼침 | ✅ | `src/renderer/stores/sidebar-store.ts:60-65` | `isProjectExpanded` = `projectExpanded[id] !== false` — undefined / true 면 펼침 |
| zustand persist (localStorage) | ✅ | `sidebar-store.ts:12-55` | `persist` middleware + `STORAGE_KEY = 'rolestra.sidebar.v1'` + `partialize` |
| 일반 채널 = 전역 (projectId NULL) | ✅ | `src/renderer/features/sidebar/GeneralChannelEntry.tsx:14,72-77` | `useGlobalGeneralChannel` hook |
| i18n 라벨 — 사이드바 구조 | ✅ | `ko.json:1182-1199`, `en.json:1184-1201` | `sidebar.aria/general/projects/project.{system,dept,freeChannels}Header` ko/en 미러 |
| i18n 라벨 — 부서명 (8 role) | ✅ | `src/shared/skill-catalog.ts:35-172` + ProjectAccordion.tsx:307 | `getSkillTemplate(role).label.ko` 경유, 8 부서 모두 |
| 회귀 — ChannelRail / ProjectRail dead | 💀 | `components/shell/index.ts:7-8`, `ChannelRail.tsx`, `ProjectRail.tsx` | production 코드 import 0건. 테스트만 살아있음. shell `index.ts` re-export 도 dead surface |
| round 3 fix — `+ 새 채널` (commit `85ffa72`) | ✅ | `ProjectAccordion.tsx:69,158-159,334-344`, `Sidebar.tsx:60,80,156-157` | `onCreateChannel?: (projectId) => void` prop drill |
| round 4 fix — abort 버튼 (commit `e7655cd`) | ✅ | `Sidebar.tsx:67,82,160`, `ProjectAccordion.tsx:73,86,131,179,210-211` | `onAbortMeeting` Sidebar→ProjectAccordion→ChannelMeetingControl prop drill. 자유 user 채널만 회의 컨트롤 표시 |
| MessengerPage.test.tsx fail 상태 | ⚠️ | `features/messenger/__tests__/MessengerPage.test.tsx` | 본 audit 범위에서 미실행. 메모리 checkpoint 의 "5 fail 의도된 vs 미해결 분리" 별도 검증 필요 |

**평가**: acceptance 4 항목 모두 ✅. dead surface (ChannelRail / ProjectRail) cleanup 후보 — T20 phase 종결에서 결정 권장. Accordion 자체 구현은 lazy mount 의 의도적 선택 — 시각 검증 후 PASS 시 허용.

### Task 9 — 일반 채널 회의 X + 새 대화 시작

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| MeetingControls hide (system_general) | ✅ | `Thread.tsx:356,365`, `ChannelMeetingControl.tsx:124` | `kind === 'system_general'` 분기 + `kind !== 'system_general' && activeMeeting` MeetingBanner suppress |
| auto-trigger 차단 (회의 X) | ✅ | `meeting-auto-trigger.ts:120` | `if (channel.kind === 'dm' \|\| channel.kind === 'system_general')` → dmResponder 위임 |
| 새 대화 시작 버튼 (archive) | ✅ | `GeneralChannelControls.tsx:54` | `invoke('channel:archive-conversation', { channelId })` + 확인 prompt + pending state + error inline |
| archive IPC backend | ✅ | `channel-handler.ts:163` + `channel-service.ts:678,692` | `archiveConversation` + 가드 (`kind !== 'system_general'` throw) |
| 1라운드 응답 보존 | ⚠️ | `src/main/index.ts:551` `roundSetting = channel.kind === 'system_general' ? 1 : 'unlimited'` | **`meeting-auto-trigger.ts:120` 의 dm-style 위임으로 unreachable (dead branch)**. 동작 결과는 spec 일치 (1턴 응답) 이지만 의미적으로 정리 필요 |

**평가**: acceptance 4 항목 모두 ✅. dead branch (`index.ts:551`) cleanup 후보.

### Task 10 — ProjectEntryView (변형 land)

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| ProjectEntryView 컴포넌트 보존 | ✅ | `ProjectEntryView.tsx:47` (251 lines) | 정의만 존재 |
| ProjectEntryView 어디서도 import X | 💀 | grep 결과 정의 파일 1개만 매칭 | 변형대로 unmount, dead component |
| Dashboard entry view 카드 hide | ✅ | `DashboardPage.tsx:137-140` 주석 "T10 의 '할 일 작성' 카드는 대시보드에서 제거" | round 2 결정 명시 |
| project:startWorkflow IPC 미land | ✅ | grep 0건 (주석 외) | shared/ipc-types.ts 등록 X — 의도된 미land |
| ProjectService.startWorkflow 메서드 미land | ✅ | project-service.ts 부재 | |
| 큐 패널 통합 (textarea + 라디오) | ✅ | `QueuePanel.tsx:303-368` textarea + `:312` radiogroup `data-testid="queue-panel-department-radio"` + `:359` "추가" 버튼 | round 4 통합 위치 정확 |
| 디폴트 시작 부서 = 아이디어 | ✅ | `QueuePanel.tsx:41` `DEFAULT_ENTRY_DEPARTMENT: RoleId = 'idea'` + `ProjectEntryView.tsx:35` | 양쪽 일치 |
| entry 메시지 INSERT (workflow 시작) | ✅ | `QueuePanel.tsx:171` `invoke('message:append', { channelId, content })` → meeting-auto-trigger 위임 | spec startWorkflow 가 message:append 로 우회 |
| **새 워크플로우 archive** | ❌ | `QueuePanel.tsx:handleAdd` | **결정적 누락** — handleAdd 는 새 task 추가만, 기존 활성 회의 archive/abort 동작 없음. 사용자가 같은 부서 채널에 두 번째 task 송신 시 동작 미정 |

**평가**: T10 변형 land 정확 — ProjectEntryView 가 dead component 로 유지, 큐 패널 단일 입력 통합 ✅. **acceptance 의 "새 워크플로우 archive" 미land** — 큐 동시성 정책 명시 필요. ProjectEntryView.tsx (251 lines) cleanup 후보 (R12-W 흡수 시점에 삭제 또는 startWorkflow IPC land 와 함께 wire-up).

### Task 11 — Composer disabled / enabled

| 항목 | 상태 | 근거 (file:line) | 비고 |
|------|------|------|------|
| disabled 분기 (부서 + 활성 회의 X) | ✅ | `use-channel-disabled-state.ts:43-49,50-55` | `kind === 'user' && role !== null && role !== 'general'` + `hasActiveMeeting` 조건. spec §11.4 일치 |
| Composer.disabled 반영 | ✅ | `Composer.tsx:197,87` | `disabled={readOnly \|\| workflowDisabled \|\| submitting}` + handleSend 가드 |
| **workflowActive runtime flag (ChannelService)** | ❌ | grep 0건 | **결정적 누락 (spec 갭)** — ChannelService 에 명시 flag 없음. renderer hook 이 `activeMeetings.some(m => m.channelId === channel.id)` 로 동치 판정. 동작 만족 but spec 명시 service-level state 미land |
| 일반 / DM / 자유 / system_* 영향 X | ✅ | `use-channel-disabled-state.ts:43-49` 가드 | hook 의 isDepartmentChannel === false 면 즉시 `workflowDisabled: false` |
| placeholder 한국어 정확 | ✅ | `ko.json:1207` `"deptDisabledPlaceholder": "할 일 큐의 할 일 작성으로 시작"` + en.json:1209 + `Composer.tsx:172` 배지 | spec §11.4 문구 일치 |

**평가**: acceptance 본질 4 항목 ✅. **workflowActive runtime flag 부재** — 백엔드 단일 source of truth 가 `meetings` 테이블의 active row. 추후 워크플로우가 회의 외 다른 trigger 로 시작하면 재평가 필요. T13~T17 부서 워크플로우 land 시 함께 정리 권장.

---

## 2. 회귀 / dead surface 정리

| 분류 | 항목 | 위치 | 처리 권장 |
|------|------|------|----------|
| dead component | `ProjectEntryView.tsx` (251 lines) | `src/renderer/features/project/` | T20 closeout 또는 R12-W 시점 — wire-up vs 삭제 결정 |
| dead component | `ChannelRail.tsx` | `src/renderer/features/messenger/` | T20 sweep — 테스트 (ChannelRail.test.tsx) 와 함께 정리 |
| dead component | `ProjectRail.tsx` | `src/renderer/components/shell/` | T20 sweep — 테스트 (ProjectRail.active.test.tsx + Shell.test.tsx 의존) 와 함께 정리 |
| dead surface | `components/shell/index.ts:7-8` ProjectRail re-export | shell 모듈 export | T20 sweep |
| dead branch | `src/main/index.ts:551` `roundSetting = channel.kind === 'system_general' ? 1 : 'unlimited'` | round 4 dm-style 위임 후 unreachable | T9 sweep / 또는 T20 |
| 회귀 (의도된 미해결) | `MessengerPage.test.tsx` 5 fail | T8 의 사이드바 통합 후 outdated | T20 sweep — 사이드바 기준 재작성 |

---

## 3. 결정적 누락 (acceptance 미충족) 정리

| # | 누락 | 영향 | 권장 처리 |
|---|------|------|----------|
| 1 | **T4 옵션 부서 createProject 인자 propagate** | 게임/일러스트 프로젝트에서 캐릭터/배경 부서 자동 생성 경로 부재. blueprint + service param 까지만 land | (a) ProjectCreateInput 에 `optionalDepartments?: RoleId[]` 추가 + onboarding step 또는 프로젝트 생성 모달에서 노출 + index.ts 호출 propagate. 또는 (b) R12-D phase 에서 처리하기로 명시 (spec §11.11 보류 결정과 일치). T20 closeout 시점 결정 필요 |
| 2 | **T10 새 워크플로우 archive 동작** | 사용자가 같은 부서 채널에 두 번째 task 송신 시 동작 미정 — 기존 활성 회의 무시 / 큐에 추가 / archive 후 새 시작 중 어떤 정책인지 불명 | T20 closeout 또는 T13 (아이디어 워크플로우) 진입 시 큐 동시성 정책 명시. 권장 = 같은 채널에 대해 active meeting 있으면 큐 add 차단 + UX 안내 |
| 3 | **T11 workflowActive runtime flag 부재** | spec 명시 service-level state 미land. 동작은 hook 의 `activeMeetings.some()` 로 동치 충족 | T13~T17 부서 워크플로우 land 시 워크플로우가 회의 외 trigger 로 시작하는 경우 (예: 인계) workflowActive 별 flag 도입 검토. 현재는 본질 충족이라 보강 후순위 |

---

## 4. spec-plan 어긋남 정리

| # | 어긋남 | 어디 | 권장 처리 |
|---|------|------|----------|
| 1 | **spec T7 (pause/resume) ↔ plan T7 (PromptComposer)** | spec §11.4/§11.7 vs plan tasks.json T7 | 사용자 결정 옵션 B (2026-05-03) — R12-W 또는 별 phase 이연. T20 closeout ADR + 구현-현황.md 에 명시 |
| 2 | T2 plan 문구 "ChannelPurpose 4 종" 오기 | plan tasks.json T2 acceptanceCriteria | T20 closeout 시점 plan 문구 갱신 — 자유 텍스트로 수정 |
| 3 | T2 plan 문구 "Channel 4 새 필드" 오기 | plan tasks.json T2 acceptanceCriteria | T20 closeout 시점 plan 문구 갱신 — Channel 3 + ChannelMember 1 분배 명시 |
| 4 | T4 plan 문구 "ProjectService 5 부서 자동 생성" | plan tasks.json T4 (ProjectService 책임이 ChannelService + index.ts hook 으로 이동) | T20 closeout 시점 plan 문구 갱신 — 책임 분리 명시 |
| 5 | T6 PathGuard 명시 vs assertInsideProjectRoot 자체 helper | spec §11.9 의 "junction realpath 비교 (CA-3 TOCTOU)" vs service.ts:67-81 | (a) 정식 PathGuard wire-up 또는 (b) `path.realpath` 추가 적용. Windows junction 위험 — 보강 권장 |
| 6 | T6 mtime 감지 vs content diff | plan tasks.json T6 acceptanceCriteria vs service.ts:126-137 | acceptance 표현 갱신 (실효 동작은 더 정확) |

---

## 5. 미land 항목 (별 phase 이연 결정)

| 항목 | 어디서 발견 / 결정 | 상태 / 이연처 |
|------|------|------|
| 회의 pause / resume IPC handler/router | spec §11.4 의 T7 + 사용자 결정 옵션 B | 미land (schema/type 만 D-A 시점 부분 land). R12-W 또는 별 phase 이연 |
| `project:startWorkflow` IPC 정식 | T10 plan acceptanceCriteria | 우회 (message:append + meeting-auto-trigger). 정식 wrapping 후속 |
| ProjectEntryView 별도 컴포넌트 사용처 | T10 변형 | dashboard / 큐 둘 다 unmount. file 보존 (onboarding step 3.5 같은 곳 재사용 가능) |
| 옵션 부서 (캐릭터/배경) UI 표면 | T4 누락 #1 | T20 closeout 또는 R12-D 보류 결정 |
| Windows junction PathGuard 보강 | T6 ⚠️ | T20 closeout 또는 별 보강 |
| `workflowActive` service-level flag | T11 ❌ #3 | T13~T17 부서 워크플로우 land 시 재평가 |
| MessengerPage.test.tsx 5 fail 정리 | T8 회귀 (의도된) | T20 sweep — 사이드바 기준 재작성 |
| 신규 직원 등록 시 모든 능력 자동 ON | round 2 결정 미진행 | onboarding step 3 매트릭스 강제 (디폴트 general only) 로 우회. T20 또는 R13 |

---

## 6. Land 된 fix bundles (round 1~4)

| commit | 내용 | 의의 |
|--------|------|------|
| `780b344` | round 1 ChannelRail 임시 prepend (T8 으로 흡수됨) | 임시 fix — T8 land 후 deprecated |
| `c8130f8` | round 2 검증 체크리스트 | 사용자 dogfooding 가이드 |
| `bf70269` | round 2 fix #2-1 + 의견 1/2/4-4 | Thread 검증 list / 큐 hide on dashboard / 카드 제거 / 큐 헤더 |
| `8fe8942` | PromptComposer fallback (#3-3 부서 회의 첫 턴 침묵) | T7 보강 |
| `80266f3` | onboarding step 3 능력 매트릭스 — 9 능력 ≥ 1명 강제 | 신규 직원 능력 결정 우회 |
| `375ad77` | Rolestra 브랜드 + 디폴트 폴더 + step 1 ArenaRoot picker | onboarding 정합성 |
| `7623eab` `6ae0648` `78dc6ce` | step 1 UI 정렬 / 줄바꿈 / 카드 간격 | onboarding UX |
| `ef65833` | step 2 카드 정리 / rescan 라벨 / step 3-5 description 중복 제거 | onboarding 정리 |
| `85ffa72` | round 3 fix — 일반 채널 race / 큐 패널에 ProjectEntryView 마운트 / 사이드바 + 새 채널 / 빈 채널명 자동 복원 | T8/T9/T10 보강 |
| **`e7655cd`** | **round 4 fix — abort 버튼 / 일반 채널 panel hide + backend dm-style / 빈 이름 frontend / 큐 정리 (단일 입력)** | 본 audit tip |

---

## 7. R12-C T12+ 진입 가능성 판정

**결론**: **가능**. T1~T11 본질 ✅, 결정적 누락 3건 (T4 옵션 부서 / T10 archive / T11 workflowActive) 은 T13~T17 부서 워크플로우 land 와 함께 정리 가능 또는 별 phase 이연 결정 가능. 다음 단계:

```
T12 OpinionService — D-B 의 OPINION_GATHERING + OPINION_TALLY 분리 (backend only, 시각 X)
T13 아이디어 워크플로우 — D-B-Light + USER_PICK + 의견 카드 list UI (시각 필요)
T14 디자인 워크플로우 — UX → UI → UX 토론 (3R cap, 시각 필요)
T15 PlaywrightSnapshotService — 디자인 결과물 PNG (시각 필요)
T16 검토 워크플로우 — OPINION_GATHERING + TALLY → 분류 카드 (시각 필요)
T17 구현 워크플로우 — designated 1명 (시각 필요)
T18 드래그 순서 + designated worker resolver (시각 필요)
T19 handoff_mode 토글 + 인계 모달 (시각 필요)
T20 ADR + 구현 현황 + closeout (sweep — 본 audit 의 누락 / 어긋남 / dead surface 정리)
```

**진입 권장 순서**: T12 (backend only) 진입 → 시각 필요 시점에서 stop → 사용자 dogfooding round 5 결과와 결합 → T13+ 진입.

---

## 8. 본 audit 의 한계

1. **vitest 실행 불가** — worktree 환경에서 `Cannot find module @rollup/rollup-linux-x64-gnu`. 단위 테스트는 정적 검증만 (코드 존재 + assert 시그니처) — Windows / 정상 환경 재실행 후 실 PASS 검증 필요.
2. **시각 검증 미실시** — T8 (Accordion) / T9 (general 채널 panel hide) / T10 (큐 패널 단일 입력) / T11 (Composer 배지) 코드 존재만 확인, 사용자 round 5 dogfooding 에서 검증 필요.
3. **MessengerPage.test.tsx fail count 미실행** — 메모리 checkpoint 의 5 fail "의도된 vs 미해결" 분리는 별도 vitest 실행 필요.
4. **spec T7 IPC handler/router land 여부 미확인** — `meeting:pause/resume` schema/type 은 D-A 시점 등록 확인했으나 handler 구현 / router 등록 / preload bridge 까지 land 됐는지는 본 audit 범위 밖.

---

## 9. 다음 액션 제안

1. **사용자 round 5 dogfooding 진입** — round 4 fix 4 항목 (abort 버튼 / 일반 panel hide / 큐 단일 입력 / 빈 이름 복원) 시각 검증.
2. **T20 closeout 시점 plan 문구 갱신** — spec-plan 어긋남 #2~#4 plan 측 표현 수정.
3. **T20 sweep dead surface 정리** — ChannelRail / ProjectRail / index.ts:551 dead branch / MessengerPage.test.tsx 재작성.
4. **결정적 누락 3건 결정** — T4 옵션 부서 propagate / T10 archive / T11 workflowActive — T20 또는 별 phase 이연.
5. **T6 PathGuard 보강** — Windows junction realpath 비교 추가 또는 정식 PathGuard wire-up.
6. **R12-W 또는 별 phase scope 명시** — spec T7 (pause/resume) 흡수 결정 + T4 옵션 부서 흡수 여부.
